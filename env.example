# Hugging Face token (needed if using gated models or Inference Endpoints)
HF_TOKEN=your_token_here

# If using a hosted Inference Endpoint for N-ATLaS-LLM instead of running locally
NATLAS_ENDPOINT_URL=
